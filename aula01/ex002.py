# Usamos "def" parra definir a função, logo após colocamos o nome da função, seguida pelos parâmetros que serão utilizados nela. E abrimos a função utilizando os dois pontos (:).
def soma(a, b): 
    return a + b # retorno da função

print(soma(5, 2)) # Print para mostrar a função funcionando, somando 5 e 2.